name: Sync Skills to Skills Repository

on:
  push:
    branches:
      - 'v*'
    paths:
      - 'skills/**'
  workflow_dispatch:

jobs:
  sync-skills:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source repository
        uses: actions/checkout@v7
        with:
          path: source

      - name: Checkout skills repository
        uses: actions/checkout@v7
        with:
          repository: shinyorg/skills
          token: ${{ secrets.SKILLS_REPO_TOKEN }}
          path: skills-repo

      - name: Configure Git
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      - name: Sync skills directory
        run: |
          # Copy skills content to the shiny-client plugin in the skills repo
          if [ -d "source/skills/shiny-httpserver" ]; then
            # Remove existing skill directory to ensure clean sync
            rm -rf skills-repo/plugins/shiny-client/skills/shiny-httpserver
            mkdir -p skills-repo/plugins/shiny-client/skills
            cp -r source/skills/shiny-httpserver skills-repo/plugins/shiny-client/skills/
          fi

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v8
        with:
          token: ${{ secrets.SKILLS_REPO_TOKEN }}
          path: skills-repo
          commit-message: "Update shiny-httpserver skill from HttpServer repository"
          title: "Update shiny-httpserver skill"
          body: |
            This PR updates the shiny-httpserver skill from the [HttpServer repository](https://github.com/shinyorg/httpserver).

            **Source commit:** ${{ github.sha }}
            **Triggered by:** @${{ github.actor }}

            ---
            *This PR was automatically created by the [sync-skills workflow](https://github.com/shinyorg/httpserver/actions/workflows/sync-skills.yml).*
          branch: update-shiny-httpserver-skill
          delete-branch: true
          labels: |
            automated
            skills-update
