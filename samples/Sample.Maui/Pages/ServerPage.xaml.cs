namespace Sample.Maui.Pages;

public partial class ServerPage : ContentPage
{
    // The view model is attached by Shiny Shell from the [ShellMap] registration, so the page keeps
    // the parameterless constructor a DataTemplate needs.
    public ServerPage() => this.InitializeComponent();
}
